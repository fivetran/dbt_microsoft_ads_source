version: 2

sources:
  - name: bing_ads
    schema: "{{ var('bing_ads_schema', 'bing_ads') }}"
    database: "{{ var('bing_ads_database', target.database) }}"
    tables:
      - name: ad_history
      - name: campaign_history
      - name: ad_group_history
      - name: account_history
      - name: ad_performance_daily_report